'use strict';

module.exports = {
	async up(queryInterface, Sequelize) {
		await queryInterface.createTable('photos_categories', {
			id: {
				allowNull: false,
				autoIncrement: true,
				primaryKey: true,
				type: Sequelize.INTEGER
			},
			photo_id: {
				type: Sequelize.STRING,
				allowNull: false,
				references: {
					model: 'photos',
					key: 'id'
				}
			},
			title: {
				type: Sequelize.STRING,
				allowNull: false,
				references: {
					model: 'photos',
					key: 'title'
				}
			},
			description: {
				type: Sequelize.STRING,
				allowNull: false,
				references: {
					model: 'photos',
					key: 'description'
				}
			},
			category: {
				type: Sequelize.STRING,
				allowNull: false,
				references: {
					model: 'categories',
					key: 'category'
				}
			},
			created_at: {
				allowNull: false,
				type: Sequelize.DATE
			},
			updated_at: {
				allowNull: false,
				type: Sequelize.DATE
			}
		});
	},

	async down(queryInterface, Sequelize) {
		await queryInterface.dropTable('photos_categories');
	}
};
